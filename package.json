{
  "name": "fluxocsv",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "docker compose up --build",
    "down": "docker compose down",
    "test": "npm run test --workspace=@fluxocsv/shared"
  }
}
