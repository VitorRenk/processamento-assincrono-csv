import type { CsvReport } from './csv.js';
const money = (v: number) => new Intl.NumberFormat('pt-BR', {style: 'currency', currency: 'BRL'}).format(v);
export const reportCsv = (r: CsvReport) => 'produto,unidades,faturamento\n' + r.ranking.map(p => `"${p.name.replaceAll('"', '""')}",${p.units},${p.revenue.toFixed(2)}`).join('\n');
export const reportHtml = (r: CsvReport, fileName: string) => `<!doctype html><html lang="pt-BR"><meta charset="utf-8"><title>Relatório de vendas</title><style>body{font:16px Arial;max-width:820px;margin:42px auto;color:#17201e}h1{color:#0d4036}table{border-collapse:collapse;width:100%}th,td{padding:12px;border-bottom:1px solid #ddd;text-align:left}.box{padding:20px;background:#eff8d5;margin:20px 0}</style><h1>Relatório de vendas</h1><p>Arquivo: ${fileName}</p><div class="box"><b>Faturamento:</b> ${money(r.revenue)} | <b>Ticket médio:</b> ${money(r.average)} | <b>Itens:</b> ${r.units}</div><h2>Produtos por faturamento</h2><table><tr><th>Produto</th><th>Unidades</th><th>Faturamento</th></tr>${r.ranking.map(p => `<tr><td>${p.name}</td><td>${p.units}</td><td>${money(p.revenue)}</td></tr>`).join('')}</table></html>`;
